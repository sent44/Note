{
  "name": "note",
  "version": "0.0.1",
  "description": "",
  "main": "src/app.js",
  "dependencies": {},
  "devDependencies": {
    "gh-pages": "^3.1.0"
  },
  "scripts": {
    "start": "npm run dev",
    "dev": "parcel --port 8000 --out-dir build app.html",
    "prebuild": "rm -rf build/*",
    "build": "parcel build --out-dir build app.html",
    "deploy": "gh-pages --remote github --dist build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sent44/Note.git"
  },
  "author": "sent44",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/sent44/Note/issues"
  },
  "homepage": "https://github.com/sent44/Note#readme"
}
